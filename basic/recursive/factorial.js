// Iterative

function factorial(num){
    if(num === 1) return 1;
    return num * factorial(num-1);
}

// Recursion

function factorial(num){
    if(num === 1) return 1;
    return num * factorial(num-1);
}
